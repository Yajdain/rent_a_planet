<div class="container">
  <div class="row">
    <% @planetoffers.each do |planetoffer| %>
    <div class="col-3 mb-4">
      <div class="card card-radius h-100" style="width: 18rem;">
        <img src="<%= planetoffer.planet_picture%>" class="card-img-top rounded-circle text-light" alt="...">
        <div class="card-body">
          <h5 class="card-title text-center"><%= link_to planetoffer.name, planet_offer_path(planetoffer), class: "text-deco"%></h5>
          <p class="card-text text-light"><%= planetoffer.description %></p>
          <div class="text-center">
            <a href="#" class="btn btn-color">Réserver</a>
          </div>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
